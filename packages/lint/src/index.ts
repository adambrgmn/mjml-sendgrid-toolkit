import mjml from 'mjml';
import {} from 'mjml-core';
import { ProjectConfig } from '@fransvilhelm/mjml-sendgrid-toolkit-core';

export async function lint(files: string[], project: ProjectConfig) {
  console.log(files);
}
